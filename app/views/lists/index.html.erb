<div class="container">
  <h1>My List</h1>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list.id) %></li>
    <% end %>
  </ul>
  <%= link_to 'Create a New List', new_list_path, class: 'btn btn-info' %>
</div>

 
